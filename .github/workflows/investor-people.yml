name: investor-people-discovery
# DISABLED — discovery is now done inline in investor-outreach.js (one Gemini call per email sent)
# This avoids the 2842-call bulk Gemini quota issue on the free tier.
on:
  workflow_dispatch:
    inputs:
      confirm:
        description: "Type YES to run (disabled by default)"
        required: true
        default: "NO"

jobs:
  disabled:
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.confirm == 'YES' }}
    steps:
      - run: echo "Manually enabled — but this workflow is deprecated. Use investor-outreach instead."
